import styled from "styled-components";
import Constants from "../../../../utils/constants";

const CycleArrowWrapper = styled.div`
  button{
    border: none;
    background-color: transparent;
    color: ${Constants.textColor};
    cursor: pointer;
    font-size: 30px;
  }
`
export default CycleArrowWrapper;